const state = {
  userInfo: null
};

const getters = {};

const mutations = {
  setUserInfo (state, payload) {
    state.userInfo = payload;
  },
};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
